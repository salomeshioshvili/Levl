/** Space for floating pill tab bar + safe area */
export const TAB_SCENE_PADDING_BOTTOM = 112;

/** Horizontal inset for screen edges (readable on phones, not flush to bezel). */
export const SCREEN_EDGE_GUTTER = 28;
